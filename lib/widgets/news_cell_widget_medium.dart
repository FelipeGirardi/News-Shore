import 'package:flutter/material.dart';

import '/models/news_data.dart';

class NewsCellWidgetMedium extends StatelessWidget {
  const NewsCellWidgetMedium({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
